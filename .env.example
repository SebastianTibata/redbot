# RedBot - Configuración de ejemplo
# Copie este archivo a .env y ajuste los valores según sus necesidades

# === Puertos de los servicios ===
USER_PORT=8001
ACCOUNT_PORT=8002
AUTH_PORT=8003
TASK_PORT=8004
EXECUTOR_PORT=8005

# === Base de datos ===
DATABASE_URL=postgresql://postgres:postgres@db:5432/soadb

# === Autenticación JWT ===
SECRET_KEY=change_this_secret_key_in_production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=30

# === Sistema de Protección de Acceso ===
# Tipo de autenticación adicional: 'symmetric', 'asymmetric', o 'disabled'
APP_AUTH_TYPE=disabled

# --- Autenticación Simétrica (solo si APP_AUTH_TYPE=symmetric) ---
# Llave maestra compartida para acceso a la aplicación
# APP_MASTER_KEY=your_master_key_here

# --- Autenticación Asimétrica (solo si APP_AUTH_TYPE=asymmetric) ---
# Rutas a las claves RSA
# APP_PRIVATE_KEY_PATH=./keys/private_key.pem
# APP_PUBLIC_KEY_PATH=./keys/public_key.pem
# APP_TOKEN_VALIDITY_MINUTES=60

# === URLs de servicios (para comunicación entre servicios) ===
AUTH_SERVICE_URL=http://auth_service:8000
ACCOUNT_SERVICE_URL=http://account_service:8000
TASK_SERVICE_URL=http://task_service:8000
USER_SERVICE_URL=http://user_service:8000
EXECUTOR_SERVICE_URL=http://executor_service:8000

# === Configuración de CORS (opcional) ===
# CORS_ORIGINS=http://localhost:3000,http://localhost:8000
